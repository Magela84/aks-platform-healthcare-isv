# ============================================================
# billing-rbac.yaml — namespace-scoped RBAC
# ============================================================
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: billing-team-role
  namespace: billing
rules:
  - apiGroups: ["", "apps", "autoscaling", "networking.k8s.io"]
    resources: ["pods", "pods/log", "services", "deployments", "replicasets", "horizontalpodautoscalers", "ingresses", "configmaps"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: billing-team-binding
  namespace: billing
subjects:
  - kind: ServiceAccount
    name: billing-sa
    namespace: billing
  - kind: Group
    name: "REPLACE_WITH_BILLING_TEAM_AAD_GROUP_OBJECT_ID"
    apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: Role
  name: billing-team-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-cross-tenant
  namespace: billing
spec:
  podSelector: {}
  policyTypes: ["Ingress"]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              tenant: billing
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
